{
  "name": "${KS_NAME}-indexer",
  "description": "OneLake JSON indexer with embeddings for knowledge source '${KS_NAME}'",
  "dataSourceName": "${KS_NAME}-datasource",
  "skillsetName": "${KS_NAME}-skillset",
  "targetIndexName": "${KS_NAME}-index",
  "disabled": false,
  "parameters": {
    "batchSize": 1,
    "maxFailedItems": -1,
    "maxFailedItemsPerBatch": -1,
    "configuration": {
      "parsingMode": "text",
      "indexedFileNameExtensions": ".json"
    }
  },
  "fieldMappings": []
}